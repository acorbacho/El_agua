/**
 * Función para redireccionar a la página de inicio.
 */
function index_redirect() {
  //Método de redirección.
  window.location.replace('/index.html')
}
/**
 * Temporizador de ejecución de 3 segundos para la función anterior.
 */
setTimeout(index_redirect, 3000)